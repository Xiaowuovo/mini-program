# 🚀 低成本视频监控完整部署方案

> **项目**：共享菜园智能监控系统
> **方案**：混合模式（快照+实时）
> **成本**：硬件300元 + 运营5-50元/月

---

## 📋 总体架构

```
┌─────────────────────────────────────────────────────┐
│                  系统架构图                          │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌──────────────┐       ┌────────────────────┐    │
│  │  树莓派+摄像头│       │   云服务器/本地     │    │
│  │              │       │                    │    │
│  │  1.定时拍照  │──HTTP─>│  1. Nginx静态文件  │    │
│  │    Cron      │       │  2. 接收快照       │    │
│  │              │       │  3. RTMP服务       │    │
│  │  2.实时推流  │──RTMP─>│  4. 转HLS         │    │
│  │    FFmpeg    │       │                    │    │
│  └──────────────┘       └────────────────────┘    │
│         │                        │                 │
│         │                        │                 │
│         ▼                        ▼                 │
│  ┌─────────────────────────────────────────┐      │
│  │         微信小程序                       │      │
│  │                                         │      │
│  │  - 快照模式：<image> 显示图片            │      │
│  │  - 实时模式：<live-player> HLS播放       │      │
│  │  - 切换按钮：用户自主选择                │      │
│  └─────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────┘
```

---

## 💰 成本详细分析

### 一次性硬件成本

| 项目 | 规格 | 单价 | 数量 | 小计 | 备注 |
|------|------|------|------|------|------|
| **树莓派** | Zero 2W | 150元 | 1 | 150元 | 或4B：300元 |
| **摄像头** | USB 1080P | 80元 | 1 | 80元 | 或CSI：100元 |
| **TF卡** | 32GB C10 | 40元 | 1 | 40元 | 闪迪/三星 |
| **电源** | 5V 3A | 30元 | 1 | 30元 | 官方推荐 |
| **外壳** | 防水IP65 | 30元 | 1 | 30元 | 户外必备 |
| **合计** | - | - | - | **330元** | **单点位** |

### 月度运营成本

#### 方案A：自建服务器（推荐）

| 项目 | 配置 | 月费 | 备注 |
|------|------|------|------|
| 云服务器 | 1核2G | 30元 | 腾讯云/阿里云学生机 |
| 带宽 | 3Mbps | 包含 | 共享带宽 |
| 流量 | 50GB | 包含 | 快照<1GB，实时<5GB |
| **合计** | - | **30元** | **全部监控点共享** |

#### 方案B：使用树莓派作为服务器（最省钱）

| 项目 | 说明 | 月费 |
|------|------|------|
| 树莓派4B | 作为服务器 | 0元 |
| 家庭宽带 | 上行5Mbps | 0元（已有） |
| 电费 | 5W × 24h × 30天 | <5元 |
| **合计** | - | **<5元** |

### 10个监控点总成本对比

| 成本类型 | 本方案 | 微信小程序 | 云服务TRTC | 节省比例 |
|----------|--------|-----------|-----------|----------|
| **硬件成本** | 3300元 | 0元 | 0元 | - |
| **月运营** | 30-50元 | 600元 | 135元 | 75-95% |
| **年运营** | 360-600元 | 7200元 | 1620元 | **91.7%** |
| **3年总成本** | 4380元 | 21600元 | 4860元 | **79.7%** |

> 💡 **结论**：虽然硬件有一次性投入，但1年即可回本，3年总成本节省接近80%

---

## 🛠️ 部署步骤

### 阶段1：硬件准备（1天）

#### 1.1 购买硬件

**购买清单**（推荐店铺见附录）：
- ✅ 树莓派 Zero 2W 或 4B 2GB
- ✅ USB摄像头 1080P（罗技C270 或 奥尼A8）
- ✅ 闪迪32GB TF卡
- ✅ 官方5V 3A电源
- ✅ 防水外壳

**验收标准**：
```bash
# 检查树莓派启动
# 红灯常亮：通电正常
# 绿灯闪烁：系统运行

# 检查摄像头
lsusb
# 应该看到类似：
# Bus 001 Device 004: ID 046d:0825 Logitech, Inc. Webcam C270
```

#### 1.2 安装系统

**方式1：使用Imager（推荐）**
```
1. 下载 Raspberry Pi Imager
2. 选择系统：Raspberry Pi OS Lite
3. 选择TF卡
4. 点击"设置"图标：
   - 启用SSH ✓
   - 用户：pi / raspberry
   - WiFi：你的SSID和密码
   - 时区：Asia/Shanghai
5. 写入
```

**方式2：手动配置**
```bash
# 下载镜像
wget https://downloads.raspberrypi.org/raspios_lite_armhf_latest

# 烧录（macOS）
sudo dd if=raspios.img of=/dev/disk2 bs=1m

# 烧录（Windows）
# 使用 Win32DiskImager 或 balenaEtcher
```

---

### 阶段2：树莓派配置（2小时）

#### 2.1 首次登录

```bash
# 1. 找到树莓派IP
# 方法A：路由器管理页面查看
# 方法B：使用IP扫描工具
nmap -sn 192.168.1.0/24 | grep Raspberry

# 2. SSH连接
ssh pi@192.168.1.100  # 替换为实际IP
# 密码：raspberry

# 3. 修改密码（安全）
passwd
```

#### 2.2 一键部署

**下载部署脚本**：
```bash
# 方法1：直接下载
wget https://yourserver.com/scripts/install.sh
sudo bash install.sh

# 方法2：复制粘贴
nano install.sh
# 粘贴 raspberry-pi/install.sh 的内容
# Ctrl+O 保存, Ctrl+X 退出
chmod +x install.sh
sudo bash install.sh
```

**脚本会自动完成**：
- ✅ 系统更新
- ✅ 安装FFmpeg、ImageMagick等
- ✅ 创建项目目录
- ✅ 配置定时任务
- ✅ 测试摄像头

**预计时间**：20-30分钟

#### 2.3 手动测试

```bash
# 测试拍照
cd ~/garden-monitor
./snapshot.sh

# 查看日志
tail -20 snapshot.log

# 应该看到：
# [2024-12-01 10:00:00] 开始拍照...
# [2024-12-01 10:00:02] 拍照成功: garden1_monitor1_20241201_100002.jpg
# [2024-12-01 10:00:02] 文件大小: 245678 bytes
# [2024-12-01 10:00:05] 上传成功
```

---

### 阶段3：服务器部署（3小时）

#### 3.1 购买云服务器

**推荐配置**：
- CPU：1核
- 内存：2GB
- 带宽：3Mbps
- 系统：Ubuntu 22.04 LTS
- 价格：30元/月（学生9.9元/月）

**购买链接**：
- 腾讯云：https://cloud.tencent.com/act/campus
- 阿里云：https://www.aliyun.com/daily-act/ecs/student

#### 3.2 安装Nginx-RTMP

```bash
# 连接服务器
ssh root@your_server_ip

# 更新系统
apt update && apt upgrade -y

# 安装Nginx和RTMP模块
apt install -y nginx libnginx-mod-rtmp

# 验证安装
nginx -v
# 应该看到版本信息
```

#### 3.3 配置Nginx

**编辑配置文件**：
```bash
nano /etc/nginx/nginx.conf
```

**在文件末尾添加**：
```nginx
rtmp {
    server {
        listen 1935;
        chunk_size 4096;

        application live {
            live on;
            record off;

            # HLS配置
            hls on;
            hls_path /tmp/hls;
            hls_fragment 2s;
            hls_playlist_length 6s;

            # 只允许内网推流（安全）
            allow publish 127.0.0.1;
            allow publish 你的树莓派公网IP;
            deny publish all;
        }
    }
}

http {
    # ... 原有配置保持不变 ...

    server {
        listen 8080;

        # HLS播放端点
        location /hls {
            types {
                application/vnd.apple.mpegurl m3u8;
                video/mp2t ts;
            }
            root /tmp;
            add_header Cache-Control no-cache;
            add_header Access-Control-Allow-Origin *;
        }

        # 快照上传接口（由后端处理）
        location /api {
            proxy_pass http://127.0.0.1:5000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
```

**测试并重启**：
```bash
# 测试配置
nginx -t

# 重启
systemctl restart nginx

# 检查状态
systemctl status nginx
```

#### 3.4 配置防火墙

```bash
# 开放端口
ufw allow 1935/tcp  # RTMP
ufw allow 8080/tcp  # HLS
ufw allow 80/tcp    # HTTP
ufw allow 443/tcp   # HTTPS
ufw enable
```

---

### 阶段4：后端API部署（1小时）

#### 4.1 安装Python环境

```bash
# 安装Python3
apt install -y python3 python3-pip python3-venv

# 创建项目目录
mkdir -p /var/www/garden-api
cd /var/www/garden-api

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install flask flask-sqlalchemy pillow
```

#### 4.2 上传后端代码

```bash
# 上传方式1：scp
scp -r backend/* root@your_server:/var/www/garden-api/

# 上传方式2：git
git clone https://yourrepo.com/garden-backend.git
```

#### 4.3 配置systemd服务

```bash
nano /etc/systemd/system/garden-api.service
```

```ini
[Unit]
Description=Garden API Service
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/var/www/garden-api
Environment="PATH=/var/www/garden-api/venv/bin"
ExecStart=/var/www/garden-api/venv/bin/python app.py
Restart=always

[Install]
WantedBy=multi-user.target
```

**启动服务**：
```bash
systemctl enable garden-api
systemctl start garden-api
systemctl status garden-api
```

---

### 阶段5：小程序部署（1天）

#### 5.1 上传代码

```
1. 打开微信开发者工具
2. 导入项目
3. 配置request合法域名：
   - yourserver.com
4. 配置业务域名（直播）：
   - yourserver.com:8080
5. 点击"上传"
```

#### 5.2 配置服务器地址

**修改配置文件**：
```javascript
// miniprogram/config.js
module.exports = {
  apiUrl: 'https://yourserver.com/api',
  hlsUrl: 'https://yourserver.com:8080/hls'
}
```

#### 5.3 提交审核

```
1. 登录微信公众平台
2. 版本管理 → 提交审核
3. 填写审核信息
4. 等待1-3天
```

---

## 🧪 完整测试流程

### 测试清单

#### ✅ 硬件测试

```bash
# 1. 树莓派启动
ping raspberrypi.local

# 2. 摄像头识别
v4l2-ctl --list-devices

# 3. 拍照测试
ffmpeg -f v4l2 -i /dev/video0 -frames 1 test.jpg

# 4. 网络测试
ping yourserver.com
```

#### ✅ 快照功能测试

```bash
# 1. 手动拍照
cd ~/garden-monitor
./snapshot.sh

# 2. 检查日志
tail snapshot.log
# 应该看到"上传成功"

# 3. 检查服务器
# 访问：https://yourserver.com/uploads/snapshots/
# 应该能看到刚上传的图片

# 4. 小程序查看
# 打开小程序 → 视频监控 → 应该显示最新快照
```

#### ✅ 实时推流测试

```bash
# 1. 树莓派推流
cd ~/garden-monitor
./stream.sh

# 2. 检查进程
ps aux | grep ffmpeg

# 3. 检查服务器HLS文件
ssh root@yourserver
ls -lh /tmp/hls/
# 应该看到 .m3u8 和 .ts 文件

# 4. 小程序播放
# 切换到"实时模式"
# 应该能看到实时画面（延迟3-10秒）

# 5. 停止推流
./stop_stream.sh
```

#### ✅ 定时任务测试

```bash
# 查看crontab
crontab -l

# 等待15分钟，检查是否自动拍照
tail -f ~/garden-monitor/snapshot.log

# 应该每15分钟看到一次拍照记录
```

---

## 📊 性能指标

### 快照模式

| 指标 | 数值 | 说明 |
|------|------|------|
| 分辨率 | 1280x720 | 高清 |
| 图片大小 | 150-250KB | 压缩后 |
| 拍照间隔 | 15分钟 | 可配置 |
| 延迟 | 15秒-15分钟 | 取决于刷新时机 |
| 日流量 | <20MB | 96张图 |
| 月流量 | <600MB | 可接受 |

### 实时模式

| 指标 | 数值 | 说明 |
|------|------|------|
| 分辨率 | 640x480 | 标清 |
| 码率 | 500Kbps | 可调整 |
| 帧率 | 15fps | 流畅 |
| 延迟 | 3-10秒 | HLS特性 |
| 小时流量 | ~225MB | 按需使用 |

---

## 🎓 答辩要点

### 技术亮点

1. **混合架构创新**
   - 90%场景用快照（低成本）
   - 10%场景用实时（高体验）
   - 年成本降低91.7%

2. **树莓派方案**
   - 单点位硬件成本300元
   - vs 专业监控1500-3000元
   - 节省80-90%

3. **技术栈广度**
   - 嵌入式开发（树莓派）
   - 流媒体技术（RTMP/HLS）
   - 服务器运维（Nginx）
   - 前端开发（小程序）

4. **商业价值**
   - 10个监控点月成本<50元
   - vs 传统方案600-7200元
   - 可直接商用

### 演示流程

```
1. 展示硬件（树莓派+摄像头）
2. 打开小程序快照模式
3. 点击"刷新"获取最新画面
4. 切换到实时模式
5. 等待3秒看到实时画面
6. 展示成本对比表
```

---

## 🔧 运维指南

### 日常维护

```bash
# 每周检查
- 树莓派在线状态
- 磁盘空间使用
- 日志文件大小

# 每月任务
- 清理旧日志
- 检查系统更新
- 验证备份

# 故障处理
- 重启树莓派：sudo reboot
- 重启服务：systemctl restart garden-api
- 查看日志：journalctl -u garden-api -f
```

---

**部署完成！现在你拥有一个成本仅为传统方案1/10的专业视频监控系统！** ✅

---

*文档版本：v1.0*
*更新时间：2024年12月*
