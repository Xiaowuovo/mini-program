1. git reflog，如果不小心强制提交覆盖了，可以用这个恢复

2. 如果不小心在git的时候把内容全部删除了，这里很奇怪，被git追踪的文件是不会进入回收站的，这也就是说你的修改会全都不见了，这个时候还有一招，就是使用cursor或者其他idea编辑器来看历史记录；

3. cd /usr/local/coturn/etc
    ../bin/turnserver -v -r 124.222.14.2:3478 -a -o -c turnserver.conf
   /opt/janus/bin

   nohup ./janus > output.log 2>&1 &  

4. 部署，先是env，然后是db，然后装依赖，然后start.bat